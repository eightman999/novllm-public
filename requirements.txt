torch>=2.3
transformers>=4.51
datasets>=2.19
peft>=0.11
trl>=0.9
accelerate>=0.31
sentencepiece
protobuf

# bitsandbytes (QLoRA 4bit) は CUDA 専用。Mac(MPS)/CPU では未使用
# (train_lora.py は device=cuda のときのみ import する)
bitsandbytes>=0.43; sys_platform == "linux"
